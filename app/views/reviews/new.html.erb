<h1>Create a new review for <%= @restaurant.name %></h1>
<%= simple_form_for [@restaurant, @review] do |f| %>

<%= f.input :content %>
<%= f.input :rating, :collection => Review::RATING %>
<%= f.submit "Create your review", class: "btn btn-primary" %>

<% end %>
